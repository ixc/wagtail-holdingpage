[project]
name = "wagtail-holdingpage"
version = "0.1.0"
authors = [
    { name = "Cameron Simpson", email = "cs@cskk.id.au" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
